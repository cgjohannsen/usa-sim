from task import Task, Sched_Task


class RMS:

	def schedule(task_set):
		# task_set = [Task0, Task1, ... , Taskn]
		if not sched_test(task_set):
			return None

		# Create the schedule

		return schedule

	def sched_test(task_set):
		schedulable = False
		# If -- schedulability test success, return True
		# Else If -- exact_analysis is true, return True
		# Else -- return False
		return schedulable

	def exact_analysis(task_set):
		schedulable = False
		# Do exact analysis. If successful, return true. Else, false
		return schedulable
